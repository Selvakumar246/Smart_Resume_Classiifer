import { Check, Crown, Mail, ShieldCheck, UserRound } from 'lucide-react'
import { useAuth } from '../context/AuthContext'

export function ProfilePage() {
  const { user } = useAuth()
  return <div className="mx-auto max-w-5xl"><div><p className="text-sm font-bold text-blue-600">Profile & plan</p><h2 className="mt-2 font-display text-3xl font-extrabold sm:text-4xl">Manage your workspace identity</h2></div><div className="mt-8 grid gap-6 lg:grid-cols-[.85fr_1.15fr]"><section className="card"><div className="flex items-center gap-4"><span className="grid h-16 w-16 place-items-center rounded-3xl bg-gradient-to-br from-blue-600 to-violet-600 font-display text-2xl font-extrabold text-white">{user?.name.slice(0, 1).toUpperCase()}</span><div><h3 className="font-display text-xl font-bold">{user?.name}</h3><p className="mt-1 text-sm text-slate-500">Member since {user && new Date(user.created_at).toLocaleDateString()}</p></div></div><div className="mt-7 space-y-3"><div className="flex items-center gap-3 rounded-2xl bg-slate-50 p-4 dark:bg-white/[.035]"><Mail size={18} className="text-blue-600" /><div><p className="text-xs text-slate-400">Email</p><p className="text-sm font-semibold">{user?.email}</p></div></div><div className="flex items-center gap-3 rounded-2xl bg-slate-50 p-4 dark:bg-white/[.035]"><UserRound size={18} className="text-blue-600" /><div><p className="text-xs text-slate-400">Account role</p><p className="text-sm font-semibold">{user?.is_admin ? 'Administrator' : 'Job seeker'}</p></div></div><div className="flex items-center gap-3 rounded-2xl bg-slate-50 p-4 dark:bg-white/[.035]"><ShieldCheck size={18} className="text-blue-600" /><div><p className="text-xs text-slate-400">Authentication</p><p className="text-sm font-semibold">JWT-protected workspace</p></div></div></div></section><section className="relative overflow-hidden rounded-[2rem] bg-slate-950 p-7 text-white shadow-soft"><div className="absolute right-0 top-0 h-64 w-64 rounded-full bg-blue-600/20 blur-3xl" /><div className="relative"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-blue-300">Current subscription</p><h3 className="mt-2 font-display text-3xl font-extrabold capitalize">{user?.plan} plan</h3></div><Crown className="text-amber-400" size={28} /></div><p className="mt-4 max-w-md text-sm leading-6 text-slate-400">The local production scaffold enforces three analyses per calendar month for free accounts.</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{['3 monthly analyses', 'Top-5 classification', 'ATS score breakdown', 'Skill-gap roadmap'].map((item) => <div key={item} className="flex items-center gap-2 text-sm font-semibold"><Check size={16} className="text-emerald-400" />{item}</div>)}</div><button className="button-primary mt-8">Upgrade billing setup</button><p className="mt-3 text-xs text-slate-500">Connect Stripe or another provider before enabling real payments.</p></div></section></div></div>
}
